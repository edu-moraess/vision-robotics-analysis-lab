# Optional YOLO/Ultralytics deployment extra.
# Install this only when the YOLO baseline is needed; the base app uses
# opencv-python-headless and the YOLO package may request opencv-python.
ultralytics>=8.3.0
